import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/', // Volta a apontar para a raiz (ou podes simplesmente apagar esta linha)
})